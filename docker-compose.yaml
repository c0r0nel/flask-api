services:
  flask-api-app:
    build: .
    ports:
      - "5001:5001"
      - "8000:8000"
    environment:
      - API_KEY=somekey
      - REDIS_HOST=redis
    depends_on:
      - redis

  redis:
    image: "redis:latest"
    ports:
      - "6379:6379"
